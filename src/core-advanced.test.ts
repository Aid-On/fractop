/**
 * FractoP Advanced Features Tests
 *
 * Split into:
 * - core-advanced-parallel.test.ts (parallel processing tests)
 * - core-advanced-streaming.test.ts (streaming API tests)
 */

import { describe, it } from 'vitest';

describe('FractoP Advanced (split)', () => {
  it('tests moved to core-advanced-parallel.test.ts and core-advanced-streaming.test.ts', () => {
    // Tests have been split into smaller files
  });
});
