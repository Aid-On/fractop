/**
 * FractoP Core Error Handling Tests
 *
 * Split into:
 * - core-error-timeout.test.ts (timeout, retry, circuit breaker)
 * - core-error-recovery.test.ts (recovery callbacks, events, degradation)
 */

import { describe, it } from 'vitest';

describe('FractoP Error Handling (split)', () => {
  it('tests moved to core-error-timeout.test.ts and core-error-recovery.test.ts', () => {
    // Tests have been split into smaller files
  });
});
