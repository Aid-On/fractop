/**
 * FractoP Core Tests
 *
 * Split into:
 * - core-basic.test.ts (basic processing, timeout, retry)
 * - core-events-callbacks.test.ts (circuit breaker, events, parallel, streaming, callbacks)
 * - core-supplement.test.ts (error recovery, callbacks, supplement, merge utilities)
 */

import { describe, it } from 'vitest';

describe('FractoP Core (split)', () => {
  it('tests moved to core-basic.test.ts, core-events-callbacks.test.ts, core-supplement.test.ts', () => {
    // Tests have been split into smaller files
  });
});
